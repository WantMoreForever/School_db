<?php

/**
 * 上传与导入文件规则配置
 *
 * 负责范围：
 * - 头像上传大小与 MIME 类型限制
 * - 批量导入文件扩展名限制
 *
 * 修改影响：
 * - 会影响学生端、教师端、后台头像上传
 * - 会影响管理员 Excel/CSV 导入是否允许通过校验
 */

return [
    'avatar' => [
        // 头像最大上传大小，单位字节。
        // 当前值 2 * 1024 * 1024 表示 2MB。
        // 修改影响：前端上传前校验、服务端头像保存校验。
        'max_size' => 2 * 1024 * 1024,

        'allowed_mimes' => [
            // 键：浏览器 / fileinfo 识别到的 MIME。
            // 值：保存文件时允许使用的扩展名。
            // 修改影响：头像文件是否被接受，以及保存后的后缀。
            'image/jpeg' => 'jpg',
            'image/png' => 'png',
            'image/gif' => 'gif',
            'image/webp' => 'webp',
        ],
    ],

    'import' => [
        // 允许导入的文件扩展名。
        // 修改影响：管理员导入学生/教师时，文件校验是否通过。
        'allowed_extensions' => ['xlsx', 'csv'],
    ],
];
